<link rel="stylesheet" href="/css/styles.css">

<%- include('../partials/_navbar.ejs') %> <!-- Include Navbar -->

<h1>Edit Recipe</h1>

<form action="/users/<%= user._id %>/recipes/<%= recipe._id %>?_method=PUT" method="POST" class="form-container">
  <div class="form-group">
    <label for="title">Recipe Title:</label>
    <input type="text" name="title" value="<%= recipe.title %>" required />
  </div>

  <div class="form-group">
    <label for="ingredients">Ingredients:</label>
    <textarea name="ingredients" rows="4" required><%= recipe.ingredients.join(', ') %></textarea>
  </div>

  <div class="form-group">
    <label for="instructions">Instructions:</label>
    <textarea name="instructions" rows="6" required><%= recipe.instructions %></textarea>
  </div>

  <button type="submit" class="button">Update Recipe</button>
</form>

<form action="/users/<%= user._id %>/recipes/<%= recipe._id %>?_method=DELETE" method="POST">
  <button type="submit" class="button delete-button">Delete Recipe</button>
</form>

<%- include('../partials/footer.ejs') %>

