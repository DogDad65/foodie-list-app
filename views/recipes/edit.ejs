<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/CSS/styles.css">
    <title>Edit Recipe</title>
</head>
<body>

    <!-- Navigation Bar -->
    <%- include('../partials/_navbar') %>

    <!-- Main Content Area -->
    <div class="main-content">
        <h2>Edit Recipe</h2>
        <form action="/recipes/<%= recipe._id %>?_method=PUT" method="POST" class="edit-recipe-container">
            <input type="text" name="title" value="<%= recipe.title %>" required>
            <textarea name="ingredients" rows="5" required><%= recipe.ingredients.join('\n') %></textarea>
            <textarea name="instructions" rows="8" required><%= recipe.instructions %></textarea>
            <button type="submit">Save Changes</button>
        </form>
    </div>

    <!-- Footer -->
    <%- include('../partials/_footer') %>

</body>
</html>

